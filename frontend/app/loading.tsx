'use client'

export default function Loading() {
  // No mostrar nada - dejar que las páginas carguen directamente
  // Esto evita el flash de loading innecesario en navegación rápida
  return null
}